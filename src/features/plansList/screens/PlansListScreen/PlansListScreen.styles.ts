import { StyleSheet } from 'react-native';
import { gray, backgroundColor } from '../../../../styles/colors';

const styles = StyleSheet.create({
  container: {
    width: '100%',
  },
});

export default styles;
