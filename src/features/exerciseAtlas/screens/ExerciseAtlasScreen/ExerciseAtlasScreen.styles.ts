import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: '100%',
    width: '100%',
  },
  atlasWrapper: {
    gap: 16,
  },
  input: {
    marginBottom: 16,
  },
});

export default styles;
