import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: '100%',
    width: '100%',
  },
  atlasWrapper: {
    gap: 16,
    marginBottom: 16,
  },
  input: {
    marginBottom: 16,
  },
  buttonsWrapper: {
    flexDirection: 'row',
    gap: 8,
  },
  button: {
    flexShrink: 1,
  },
});

export default styles;
